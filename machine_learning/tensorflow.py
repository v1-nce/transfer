import tensorflow as tf
from tensorflow.keras.layers import Dense
from tensorflow.keras.models import Sequential

# Things to know
# 1. Dense(units=, activation=)
# 2. model = Sequential()
# 3. model.compile()
# 4. model.fit(X, y, epochs=)
# 5. model.predict(x_new)
# 6. model.summary()
# 7. model.get_layer("layer1")/.get_weights()/


# tf.keras
# 1. tf.keras.layers.Normalization(axis=)
# 2. tf.keras.Input(shape=)
# 3. tf.random.set_seed()
# 4. loss = tf.keras.losses.BinaryCrossEntropy()
# 5. tf.keras.optimizers.Adam(learning_rate=0.01)

